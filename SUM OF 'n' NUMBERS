/*SUM OF 'n' NUMBERS*/

  #include<stdio.h>
  #include<conio.h>
void main()
{
    int n,i,s=0;
      printf("Enter the value of n: ");
    scanf("%d",&n);
    
for(i=0;i<=n;i++)
      s+=i;
 printf("Sum of N nnumbers: %d",n);
 getch();
}
